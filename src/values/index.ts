import { Colors } from './Colors';
import {
  FontSize,
  MaxWidth,
  Padding as P,
  Paddings
} from './Style';

// Enums, consts
export { Colors, FontSize, MaxWidth, Paddings };

// Types
export type Padding = P;
