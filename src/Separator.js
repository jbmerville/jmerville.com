import React from 'react';

const Separator = () => {

  const styles = {
    separotor: {
      height: '15px',
      width: '100%',
    }
  };

  return (
    <div style={styles.separotor}></div>
  );
  
};

export default Separator;
